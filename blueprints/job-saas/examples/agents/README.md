# Agent examples

Set:

- `JOB_API_KEY`
- `JOB_API_URL=https://YOUR_DOMAIN/v1`
- `JOB_MCP_URL=https://YOUR_DOMAIN/mcp`

Start with a read/preparation-only key. Create a separate key with `applications:submit` only for a trusted workflow that deliberately asks for submission authority.
